const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', error => console.log('PAGE ERROR:', error.message)); await page.goto('http://localhost:5173'); await page.evaluate(() => { localStorage.setItem('token-desa-cantik', 'dummy'); localStorage.setItem('username', 'admin_pusat'); localStorage.setItem('token-pusat', 'dummy'); }); await page.goto('http://localhost:5173/admin/desa/SIMOKETAWANG'); await new Promise(r => setTimeout(r, 5000)); await browser.close(); })();
